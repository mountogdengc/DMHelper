==============================================================================
v3.7 BUGS (Highest Priority)
==============================================================================

- Daggerheart: "dead at zero HP" logic is wrong - needs to connect HP to HP
  (not just to the death threshold)
- Publish button reactivation not always working for combats - switching back
  and forth between combats fails to reactivate the button reliably
- Bestiary fiddling doesn't work right - both the default bestiary and the
  user bestiary have issues; new campaign doesn't open the newly selected
  bestiary after switch
- Add monster to a location - always places the token at the center of the
  map instead of at the correct position
- Cast a spell (Fireball) does nothing. Local spellbook has effects placed at
  the wrong relative position - check the default spellbook and any new
  copy/paste behavior
- VLC stale cache handling - stale VLC cache causes problems; needs proper
  detection and clearing


==============================================================================
ACTIVE BUGS
==============================================================================

- Typing in a text entry is very slow
- Publishing text doesn't scale, animate, or publish correctly
- Effects highlight player tokens even when the effect is not active, if a
  token is moved. Moving the effect clears it but it re-activates on the next
  token move. This leaks information about hidden traps/status effects to
  players who can see tokens light up unexpectedly
- Map Markers: only 1/4 of marker visible to players; right-click Edit and
  Delete don't work; marker cannot be scaled small enough (becomes highly
  pixelated); text becomes unreadable at appropriate map scale
- Multiple monster tokens: clicking "Edit current token" always opens only the
  first image in the list, regardless of which token is selected (e.g. Bandit
  with multiple token images)
- Mac only: video playback entirely broken - adding a video (MP4) via the
  campaign tree shows "not able to read image file"; only images are supported
- Dropping an image in a battle comes out distorted in Player View but is
  correct in DM View
- Spellbook: cannot select created spells to modify them
- Spellbook: arrows on the right side don't work to cycle through spells
- Copy character is broken: Hit Points and AC not copied, Reactions not copied
  (confirmed for Noble), Monster Type mapped to Race instead of Class, Speed
  possibly missing too
- Export dialog shows empty entries in the tree
- Clear Icon button doesn't work
- Don't auto-save if the filename hasn't been chosen yet
- Individual entries can hide overlays (e.g. for video spots)
- New entry (PC, maybe anything) should focus/highlight the entry name field
  immediately for editing
- Rendering distance label uses very tiny font
- Populated icon doesn't show up in bestiary until you change monsters (needs
  a refresh)
- CRASH: updating Bestiary from default to a new one while a battle is open
- CRASH: recording/using cloud effect layers
- CRASH: changing the bestiary with a campaign open (e.g. effect layers are
  affected)
- Effect selection highlight is not shown
- EncounterTextEdit: Clear does not clear the background
- Fix image filters to include all images and videos (not just one type)
- When switching menus, the active tool is deselected unexpectedly
- Remove then sort in initiative -> crash
- Scroll zoom also scrolls the view (should only zoom)
- Movement ring size is not scaled correctly relative to token scale
- Something is wrong with the DM side of the scrolling text display


==============================================================================
DAGGERHEART
==============================================================================

- HP needs to count down (current implementation is wrong for Daggerheart)
- Make initiative entries custom: add config to convert a resource number into
  checkboxes (HP and Stress)
- Show a range-finder overlay with circles for Close, Near, and Far range
- Show a move circle during the spotlight phase, based on the character's
  original position and the Close range radius


==============================================================================
PENDING TODOS
==============================================================================

Bestiary
- Create a new clean Bestiary file per release; make sure it's clean on
  fresh installs
- Thoroughly test replacing an existing bestiary with a new one; ensure a
  backup is created specifically when an upgrade is attempted
- Update the warning message shown during bestiary upgrade to mention the
  bestiary by name
- Populated icon doesn't appear until monster is changed - needs a force
  refresh after load
- Add search string filter to ruleset/campaign specifics
- Move some options around in the bestiary UI

Multiple icons for a monster (partially implemented for PCs, still needed
for monsters)
- Save and load multiple icons per monster entry
- Search for multiple icons with same base name automatically
- Search for multiple icons within a directory by base name
- Handle multiple icons in bestiary UI, including viewing all
- Add icon selection to the Add Monster dialog
- Add option to randomize icon when adding multiple monsters of the same type

UI / UX
- Campaign notes - add instructional text explaining how to take quick notes
- Add a "Did You Know" tip section to the welcome page (populate with as
  many tips as possible)
- Move the grid lock buttons to the battle tools tab
- Remember different grid sizes for different external monitors
- Add What's New section to the About dialog
- Need a better Help icon (current one looks bad - consider integrating a
  dragon symbol)
- Add a refresh button to the files list in the Options dialog
- Move some options to more logical locations
- Replace pseudo-wizard flows with a proper "Add New Item" dialog;
  clean up mainwindow newbattle function
- Party alt icon path should be stored as relative, not absolute
- Save new campaign: default filename should be the campaign name
- Check and audit volume usage across all audio classes

Video / Media
- Fix Map initialization to differentiate between video and image layers and
  generate a screenshot thumbnail itself
- Videos don't work on Mac - fix video playback on macOS (reported by Tharky)
- Rendering/positioning is wrong on Mac (reported by ongakugainochi)
- Messed up video rendering when Effect layers are active at the same time
- Exporter should copy video files locally and update references
- Add an "Add Video Object" button, or auto-detect videos vs images when
  using the "Add Object" button
- Check tab order in the video object options dialog
- Lazy load images for Spells & Monsters (performance)
- QImageReader::setLimit controls and improvements

Layers
- Update order/opacity/visible changes to hit both Player and DM views
- Add opacity sliders to the layer dialog
- Updates made in the Layer Dialog should apply to both views immediately
  (token visibility changes are not currently reflected)
- Scaling and positioning of layers (images/videos) of different sizes

Text / Export
- EncounterText: complete import/export of extended text capabilities


==============================================================================
FEATURES (S = Small, M = Medium, L = Large)
==============================================================================

S  Dedicated effect settings dialogs for each effect type (cone, cube, etc.)
S  Snap to grid option for tokens (position snap as needed, not forced)
S  Allow manual drag-to-scale of objects (currently only whole-number input)
S  Select multiple tokens -> right-click -> make all visible/known at once
   (having a room full of monsters to reveal one by one is painful)
S  Double-click on a combatant widget centers the map on that combatant
S  Double-click on an effect token opens the edit dialog for that token
S  Name layers based on filename only, not the full path
S  Make the grid button a flat pushbutton that opens its own popup menu
S  Mac: pinch-to-zoom on trackpad
S  Mac: standard keyboard shortcuts (Cmd-W to close window, etc.)
M  Timer overlay: integrate into the Player Window rather than a floating
   window that causes multi-monitor issues when maximized
M  Ability to manually reset Combat Statistics; also investigate why they
   appear to reset on their own randomly
M  Add ability to place text/overlay annotations on maps (both combat and
   regular maps) visible in DM View only, for quick notes, room numbers, etc.
M  Map Markers for Encounters: either add map marker support to encounter
   view, or surface the existing map markers from the map into the encounter
M  Show player HP totals on the Player Screen
M  Undo (Ctrl-Z / Cmd-Z) in the text editor
M  Undo/redo specifically for FOW drawing (separate from text undo)
M  Change token size on the fly from the map (Enlarge/Shrink) without going
   back to a dialog
M  Swap icon for an individual character token (update icon, or change form)
M  Distance unit selection (ft / yards / miles) for large-scale maps
M  Switch between party overview icon and individual player tokens in a battle
   (toggle initiative display mode)
M  Lighting-based FOW layers: torch radius or light-source radius defines
   what is revealed (rather than manually painting fog)
L  Better text editor: fix issues with pasted line spacing that can't be
   changed, and fix color picker not applying black when one character is
   already black
L  Spell check in the text editor (built-in or OS-level)
L  Drag-and-drop reordering of special abilities/actions/reactions in the
   bestiary


==============================================================================
BIG ROCKS (Longer-term)
==============================================================================

- Video-based combat objects: videos as first-class objects on the battle
  map (positioned, scaled, looping), not just background layers
- Real mini support: locked-scale tokens representing physical miniatures
  at a 1:1 table scale
- UI redesign: general overhaul of menus, toolbars, and layout
- Asset library: central browser for images, videos, and audio assets
  reusable across campaigns
- Character/Monster Templates: full generic template system replacing
  hard-coded Character class
    * Template backwards compatibility: convert Notes/Equipment/Spells to
      HTML CDATA; combine level/class fields; move spell slots to resource
      list; fix DnDBeyond import; implement findKeyForSkillName; replace
      magic string constants
    * Add Combatant dialog templates
    * Initiative list entry templates (rollover frame, conditions, events
      like lair actions, resettable per-round resources like legendary actions)
- Set scale for tabletop/physical screen setups
- Enhanced Soundboard reboot: two distinct modes needed
    Music mode: behaves like a music player (Spotify-style) - playlist
      queue with shuffle and repeat (per-song or per-list); one track at
      a time
    Ambience mode: multiple simultaneous audio layers mixed together;
      supports continuous ambient loops (e.g. creaking ship, waves) plus
      infrequent one-hit sounds triggered manually or on a long timeout
      (e.g. whale call every few minutes); user controls the mix
    These are likely best as two separate tabs in the Soundboard panel
    Video references for effects:
      Water ring splashes: vecteezy.com/video/1803905
      Smoke loop: vecteezy.com/video/14196883
      Shockwave/smoke splash: vecteezy.com/video/23951445
- Multiple publish targets (more than one player screen)
- Redo documentation, demos, and intro videos
- Map Library


==============================================================================
USER GUIDE (All items still outstanding - no updates made yet)
==============================================================================

pg. 9   Scroll Text no longer exists
pg. 10  Show Text is no longer a tool option (note: can publish from text
        entry instead - guide needs updating)
pg. 10  Translate is now under the Entry ribbon, not the Tools ribbon
pg. 11  Zoom 1 button does not exist
pg. 14  Spell Effects - last line says "See more pg x" - missing page number
        (believed to be pg. 27)
pg. 16  Text Background button does not exist
pg. 17  Publish Options (Publish Visible, Publish Zoom) are now under Player
        Camera View section, not the right-side publish buttons area
pg. 17  Publish Preview does not exist
pg. 29  Player Window initiative tracker icons are on the Left, not Right
pg. 32  Typo: "In Party View you can party can be given a token image"
pg. 34  Stray "1" in sentence: "and import 1 the character icon as well"


==============================================================================
CODE CLEANUP
==============================================================================

- Remove BattleDialogModelMonsterCombatant if no longer needed
- Clean up all commented-out dead code in battleframe.cpp, layertoken.cpp,
  layertoken.h, map.cpp, layerfow.cpp
- Fix emit itemMoved - second parameter missing/wrong
- Audit BattleFrame::handleEffectChanged(QGraphicsItem* effectItem)
- Search and resolve all remaining // TODO: Layers comments
- Investigate accidental movement circle trigger on right-click release
- Refactor backend queries into a class to automate versioning and
  debug query management
